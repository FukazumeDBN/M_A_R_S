# M.A.R.S. 開発要件

M.A.R.S.（Minecraft Administration & Runtime Supervisor）は、Ubuntu系Linux上でMinecraftサーバーを安全に監視・操作・自動運用するためのローカルGUIアプリケーションである。

## 文書情報

| 項目 | 値 |
| --- | --- |
| 状態 | Draft 2.9（全体監査・安定性改善） |
| 最終更新 | 2026-09-03 JST |
| 対象プロジェクト | `mars` |
| GitHubリポジトリ | `https://github.com/FukazumeDBN/M_A_R_S`（非公開） |
| 進捗DB | `/home/chappy/empire/data/empire.sqlite3` |
| 開発メモ | `/home/chappy/empire/projects/mars/DEVELOPMENT_NOTES.md` |
| 進捗DBプロジェクト | 新規作業は`mars`、既存履歴は`minecraft-server` |
| 直近の完了タスク | Task 18〜34 |
| 現在の作業 | なし |

この文書は製品要件・非機能要件・設計方針の正本とする。進捗、作業状態、短い作業記録、設計判断は進捗DBを正本とする。両方を用いることで、会話履歴やセッションが失われても開発を再開できる状態を保つ。

## 基本設計思想

M.A.R.S.の本質は、ターミナルで行うMinecraftサーバー操作をGUIボタンで肩代わりすることである。GUIの右コンソールと、その背後にある仮想ターミナルを中核とする。

1. サーバーディレクトリを登録する。
2. 登録ディレクトリを作業ディレクトリとする仮想ターミナルをtmux等で準備する。
3. Start、Stop、RestartなどのGUI操作は、仮想ターミナルへ対応コマンドを送信する。
4. 右コンソールはVTEで仮想ターミナルへattachし、実画面の表示とユーザーからの直接入力を提供する。
5. 自動再起動、バックアップ、監視、MOD管理などは、この中核ランタイムを利用する拡張機能とする。

初期バックエンドはtmuxとする。screen等を後から追加できるインターフェースを設け、GUIや拡張機能がtmuxコマンドへ直接依存しないようにする。

## 実装状況

細かな現在値、コードの責務、監査結果、実環境確認待ちは`DEVELOPMENT_NOTES.md`を正本とする。2026-09-03時点で、次のMVP基盤を実装している。

- `run_mars.sh` からGTK GUIを起動できる。
- 左ナビ、中央のOverview/Server/Automation画面、右コンソール、下部ステータスバーを実装した。
- 左ナビは参考画像に合わせて細い縦帯の正方形アイコンだけにし、カーソル位置でツールチップに画面名を表示する。
- 左ナビは64px固定幅のドラッグ不可なレールとし、画面表示やコンソールの幅に応じて拡縮しない。
- コンソールは通常時に最新出力へ追従し、マウスホイールで最大10,000行の履歴をスクロールできる。履歴を上へ閲覧している間は末尾への強制追従を止め、最下部へ戻ると自動追従を再開する。
- コンソールはLinux Mint端末を意識した黒背景・明色文字とし、INFOは明色、WARNは黄色、ERRORは赤色で行全体を表示する。
- 配色・枠線・ボタンは白背景、黒い細枠、角丸なし、灰色ボタン、黒いコンソールを基調にした。
- OverviewにOnline/Offlineの大表示、Port/Players/Mods、TPS/Pingを配置する。Onlineは緑、Offlineは赤とする。
- Overviewのステータス値は、項目名と長方形の値フィールドを縦に並べる。Online/Offlineの大表示には枠線を付けない。
- コンソールの親パネル、スクロール領域、VTE端末のすべてを黒背景に固定する。
- Overview上段を左から「Offline/Online、Port/Players/Mods、TPS/Ping」の順に横並びにする。下半分は将来機能用に確保する。
- 上段ブロックの固定幅・ラベル幅・余白は中央ペイン内に収まるようにし、Overviewタイトル、Offline/Online、操作ボタンをクリップさせない。
- アプリ独自のヘッダーバーは使わず、Linux Mintのウィンドウマネージャーによる標準タイトルバーを使用する。
- 下部ステータスバーは常に1行で末尾を省略表示する。非同期処理の長い結果や改行をそのまま渡して、コンソールを含むメイン領域の高さを変化させてはならない。
- tmux仮想ターミナルをサーバーディレクトリ内で準備し、開始・停止コマンドを文字列ではなくtmuxの引数リストで安全に送る中核ランタイムを実装した。
- 右コンソールは`latest.log`の反復読み込みを廃止し、VTE端末からtmuxセッションへ直接attachする。GUIボタンと端末直接入力は同一セッションを操作する。
- M.A.R.S.起動時に管理tmuxセッションを自動準備し、通常終了時はMinecraftの正常停止確認後に管理tmuxセッションも終了する。
- MinecraftがOnlineまたは起動途中の状態でウィンドウを閉じる場合は、終了確認ダイアログを表示する。YESでは正常停止完了後に終了し、NOではサーバーとウィンドウを維持する。
- 正常停止を確認できない場合はアプリ終了を中断し、強制終了によるワールド破損を避ける。
- M.A.R.S.稼働中にVTEのtmux attachが切れた場合は、管理セッションを再確認して自動再接続する。
- 管理tmuxセッションではマウス操作を自動的に有効化し、右コンソールのホイール操作をtmuxの履歴スクロールへ渡す。
- M.A.R.S.コード・テスト・文書を`projects/mars`へ移し、Forgeサーバー本体を`projects/minecraft-server/server`に残して物理分離した。
- Serverタブにディレクトリ選択、tmuxセッション名、Start/Stop/Restart、最小・最大メモリ、任意JVM引数と適用操作を実装した。tmuxバックエンドと開始・停止コマンドはM.A.R.S.内部の既定値として管理する。
- Server directoryとバックアップ保存先は、パス入力に加えてGTKのフォルダー選択ダイアログから選択できる。
- Browse操作は`Gtk.FileChooserNative`を使い、Linux Mintのデスクトップ標準または設定済みportalのフォルダー選択UIを利用する。
- JVM設定は停止中だけ`user_jvm_args.txt`へ原子的に適用し、適用前ファイルを`user_jvm_args.txt.mars-backup`へ退避する。
- JVM設定の適用完了時はJVM引数全文ではなく、反映先を示す簡潔なステータスを表示する。
- 3番目のMods / Configタブを将来機能用のプレースホルダーとして確保した。
- Automationタブに自動再起動の有効トグルと曜日・時刻、自動バックアップの有効トグル、再起動連動または独立曜日・時刻を実装した。
- 旧来の数値間隔キーを含む設定JSONも安全に読み込みつつ、新しい設定は月〜日のトグルボタンで複数曜日を選び、24時間表記の時刻とともに保存する。廃止キーは次回保存時に除去し、systemdユーザータイマーは`OnCalendar`を使用する。
- 再起動連動バックアップは再起動側の曜日・時刻を表示・保存上の正本とし、連動中のバックアップ設定へ即時同期する。実行時は停止完了確認→バックアップ→起動の順序を守る。
- 設定をXDGユーザー領域のJSONへ保存し、systemdユーザーservice/timerを生成・適用する処理を実装した。
- restart・backup timerの適用または設定保存に失敗した場合は、以前の設定から両timerを再生成して部分反映を可能な限りロールバックする。
- ワールドを安全保存して一時アーカイブから確定するバックアップ、SHA-256マニフェスト、世代保持を実装した。
- バックアップと再起動のプロセス間排他、同秒バックアップ名の衝突回避、systemd特殊文字対策を実装した。
- コア自動テスト、GUI・tmuxライフサイクル確認、実ForgeサーバーのRestart確認を通過した。現在のテスト件数と未確認項目は開発メモへ記録する。
- Restartはポート閉鎖だけで完了扱いせず、tmuxペインshell配下の全子プロセス終了と0.5秒の安定待機を確認してからStartを送る。
- 自動再起動は予定時刻の30分前にworkerを起動し、30分・10分・5分・3分・2分・1分・30秒・10秒前の警告を送る。10分前と1分前は`title @a title`、その他は`/say`相当の`say`コマンドを使い、予定時刻になってから安全な再起動を実行する。
- 自動化serviceは長時間の警告待機・バックアップを許容する明示的なタイムアウトを持ち、timerは10秒前警告に必要な精度で起動する。警告待機中は操作ロックを占有せず、M.A.R.S.終了で管理tmuxが消えた場合は再作成せず予定再起動を中止する。
- 稼働中バックアップは`save-all flush`後の新しい保存完了ログを待ち、失敗時にも`save-on`の復旧を試みる。
- Minecraftの起動・停止途中は安全な静止状態ではないため、バックアップとJVM設定適用を拒否する。
- Overviewのプレイヤー数はtmux画面を毎秒キャプチャせず、`latest.log`の新しい追記だけを読み取って更新する。

まだ実環境でsystemdタイマーを有効化していないため、自動再起動・定期バックアップの運用確認は次段階とする。クラッシュ復旧、復元GUI、MOD/Config画面も未実装である。

## 引き継ぎ・更新ルール

作業開始時は次の順で現在地を確認する。

```bash
cd /home/chappy/empire
./bin/empire dashboard
./bin/empire task list --all --project mars
sed -n '1,240p' projects/mars/DEVELOPMENT_NOTES.md
```

運用ルールは以下とする。

1. 実装前に対象タスクを進捗DBで `in_progress` にする。
2. 要件、受け入れ条件、対象範囲が変わったら、この文書を同じ作業単位で更新する。
3. 技術選定や安全性に関わる判断は `empire decision add` で記録する。
4. 調査結果や次セッションに必要な情報は進捗DBと`DEVELOPMENT_NOTES.md`へ記録する。
5. 作業終了時はタスク状態を更新し、未完了なら残作業と次の一手をDBへ残す。
6. DB変更前のバックアップが必要な場合は `./bin/empire db backup` を実行する。

## 開発時の参照環境

| 項目 | 値 |
| --- | --- |
| Minecraft | 1.20.1 |
| Forge | 47.4.10 |
| Java | OpenJDK 17 |
| サーバーディレクトリ | `/home/chappy/empire/projects/minecraft-server/server` |
| 接続先 | `127.0.0.1:25565` |
| 現在のプロセス管理 | tmuxセッション `Kichi_Craft_Pre` |
| 外部公開 | なし |
| 起動・停止 | `scripts/start-server.sh` / `scripts/stop-server.sh` |
| ログ | `server/logs/latest.log` |

変動するサーバー設定と運用状態は`DEVELOPMENT_NOTES.md`へ記録する。開発ではこの環境を壊さず、既存スクリプトとtmuxを通じて操作する。サーバー配置変更やsystemdサービスへの完全移行は、バックアップと移行手順を用意した後の別タスクとする。

## アプリ・サーバー分離方針

M.A.R.S.アプリとMinecraftサーバー本体を、次の2つの独立したプロジェクトディレクトリとして管理する。

```text
/home/chappy/empire/projects/
├─ mars/                         # GUI、バックエンド、worker、テスト、開発文書
│  ├─ app/
│  ├─ tests/
│  ├─ run_mars.sh
│  ├─ README.md
│  ├─ DEVELOPMENT_REQUIREMENTS.md
│  └─ DEVELOPMENT_NOTES.md
└─ minecraft-server/             # Forgeサーバーとサーバー単体の運用資材
   ├─ server/                     # 現在のワールド・Forge実体（移動しない）
   ├─ scripts/
   ├─ forge-installer.jar
   └─ README.md
```

分離ではサーバー本体、ワールド、Forgeライブラリを移動しない。M.A.R.S.側のコード、テスト、起動スクリプト、開発要件だけを`projects/mars`へ移す。既存のXDG設定`~/.config/mars/settings.json`に保存された絶対サーバーパスを維持する。

アプリは自身の配置位置から`server/`を推測してはならない。未登録状態を明示的に扱い、初回起動時はユーザーがServer画面からサーバーディレクトリを選択する。既存ユーザーについては保存済みの絶対パスをそのまま引き継ぐ。

物理分離は完了済みである。今後もM.A.R.S.のGit管理にはGUI、バックエンド、worker、テスト、文書だけを含め、Minecraft本体、ワールド、個人設定、バックアップ、進捗DBを含めない。

## 対象プラットフォームと技術方針

- 対象OSはUbuntu系Linuxとする。
- アプリ本体はPython 3.12で実装する。
- GUIはUbuntu環境に導入済みのPyGObject / GTK 3を使用し、GTK CSSで参考画像に近い外観を作る。
- GUI、Minecraft操作、スケジュール、バックアップ処理を分離する。M.A.R.S.終了中は自動workerが管理tmuxセッションを再作成せず、処理を安全にスキップする。
- 定期処理は `systemd --user` のservice/timerから、同じアプリのCLIワーカーを実行する。
- Minecraft操作は交換可能なTerminal backendにし、初期はtmuxを使用する。将来のscreen、systemdサービス化、RCON対応でGUIを書き直さない設計にする。
- 設定と実行履歴はXDG準拠のユーザー領域へ保存する。秘密情報をソースコードや進捗DBへ保存しない。
- root権限を前提にしない。外部ポートを自動で開放しない。
- コアロジックはGUIから独立させ、実サーバーなしでも自動テストできるようにする。

想定する構成は以下とする。

```text
GUI (GTK) ─ Application services
  ├─ Server runtime ─ Terminal backend interface ─ tmux ─ Minecraft server process
  │                                           └─ screen（将来）
  ├─ Extension registry
  │   ├─ Scheduled restart
  │   ├─ Backup
  │   └─ Other extensions（将来）
  └─ Monitor service ─ port/terminal/status collection
```

GUIはtmux、systemd、ファイルシステムへ直接アクセスせず、アプリケーションサービスを通して操作する。拡張機能はサーバーランタイムの公開操作だけを利用する。

## UI・デザイン要件

参考画像は次の2点とする。

- `/home/chappy/ピクチャ/4.jpg`: 全体レイアウト
- `/home/chappy/ピクチャ/5.jpg`: 左タブの内容と画面分類

基本レイアウトは以下とする。

- 左端: アイコンだけを囲んだ正方形ボタンの縦型ナビゲーション。画面名はツールチップで表示する。
- 中央: 選択中の操作・設定画面。
- 右側: Linux Mintの端末に近い黒背景のMinecraftサーバーコンソール。INFOは明色、WARNは黄色、ERRORは赤色で行全体を表示する。通常は末尾へ追従し、ホイールで履歴を閲覧している間は表示位置を保持する。
- 下部: Online/Offline、Minecraft/Forgeバージョン、プレイヤー数、TPS、Pingなどの簡易ステータス。
- ウィンドウはリサイズ可能とし、最低サイズでも主要操作と状態を欠落させない。
- 現代的なダークテーマや角丸カードより、参考画像のWindowsレガシーアプリ風の白背景、黒枠、灰色ボタンを優先する。
- 危険操作には確認画面を表示し、処理中は二重実行を防止する。
- 自動処理には「有効/無効」「次回実行時刻」「直近の結果」を必ず表示する。
- OverviewではOnline/Offlineを大きく表示し、Onlineは緑、Offlineは赤にする。Port・Players・Modsは項目名と長方形値を縦に並べ、TPS・Pingは区切った別グループとして縦に並べる。
- Overviewの表示順は左からOffline/Online、Port/Players/Mods、TPS/Pingとし、操作ボタンを含む上段以外の下半分は後続機能のために空けておく。

初期ナビゲーションは次の順序とする。

1. Overview
2. Server: サーバー登録、ターミナル設定、起動・停止・再起動、JVM設定
3. MOD / Config管理（将来実装のため空けておく）
4. Automation: 自動再起動・自動バックアップ
5. ステータス監視（将来）

## MVP機能要件

### 1. Overview・監視

- 現在のサーバーディレクトリを登録・検証できる。
- 登録時に、そのディレクトリを作業ディレクトリとする仮想ターミナルを準備できる。
- 開始コマンド、停止コマンド、ターミナルバックエンド、セッション名を設定として保持できる。
- サーバーの起動状態、PID、待受ポート、稼働時間を表示できる。
- VTE端末を登録済みtmuxセッションへattachし、右コンソールへ実画面を表示する。表示位置は通常時に最新出力へ自動追従する。
- 右コンソールはマウスホイールで最大10,000行の履歴を上下にスクロールできる。上方向へ手動スクロールしたら自動追従を一時停止し、最下部へ戻ったら再開する。
- tmuxのマウス操作はM.A.R.S.が管理セッション単位で自動的に有効化し、ユーザーにtmux設定を要求しない。
- 右コンソールはキーボード入力を受け付け、サーバー稼働中はMinecraftコンソール、停止中はtmux内のシェルを直接操作できる。
- GUI操作を主操作経路としつつ、同じtmuxセッションへの直接入力も補助操作として許可する。
- 起動、正常停止、手動再起動をGUIから実行できる。
- Startは開始コマンド、Stopは停止コマンドを登録済み仮想ターミナルへ送信する。
- 操作結果と失敗理由を画面上で確認できる。

### 1.1 Server登録・JVM設定

- サーバーディレクトリをパス入力またはOSネイティブのフォルダー選択ダイアログで登録できる。
- セッション名を登録できる。ターミナルバックエンドはtmux、開始コマンドは`./run.sh nogui`、停止コマンドは`stop`を内部既定値として使用する。
- 登録操作で仮想ターミナルを準備する。
- Start、Stop、RestartをServerタブからも実行できる。
- JVMの最小メモリ（`-Xms`）と最大メモリ（`-Xmx`）を個別に設定できる。
- 任意のJVM引数を複数行の入力欄へ記述できる。
- 「適用」操作で、設定をサーバーディレクトリの`user_jvm_args.txt`へ原子的に反映する。
- 最小メモリが最大メモリを超える設定、形式が不正なメモリ値、重複する`-Xms`/`-Xmx`を拒否する。
- JVM設定の反映はサーバー停止中だけ許可し、元ファイルの復旧用コピーを保持する。

### 2. 自動再起動の設定

- 自動再起動を有効・無効にできる。
- 有効・無効はトグルスイッチで変更する。
- 月〜日のボタンを複数選択して再起動曜日を設定できる。黒いボタンをEnable、白いボタンをDisableとし、時刻（HH:MM、24時間表記）は選択曜日に共通とする。少なくとも週1回の予定実行を設定できる。
- 設定した再起動曜日・時刻を正本とし、systemdユーザータイマーは最初の警告を送る30分前へ`OnCalendar`を補正する。日付をまたぐ場合は前日の曜日へ補正する。
- 次回実行予定をGUIで確認できる。
- 再起動前にゲーム内警告を送る。予定時刻の30分・10分・5分・3分・2分・1分・30秒・10秒前に送信し、10分前と1分前は`title`、それ以外は`say`を使う。警告はMinecraftがオンラインの場合だけ送信し、オフライン時にtmux内のシェルへ誤送信しない。
- 再起動は正常停止、ポート閉鎖、tmuxペインshell配下の全子プロセス終了、0.5秒の安定待機、起動の順で行う。停止完了前にStartコマンドを送ってはならない。
- 同じ処理の重複実行をロックで防止する。
- 失敗時は無限再試行せず、回数制限とバックオフを設けて履歴へ記録する。
- systemdユーザータイマーのworkerは、M.A.R.S.管理tmuxセッションが存在するときだけ処理する。M.A.R.S.終了中はセッションを再作成せずスキップする。
- M.A.R.S.終了時はMinecraftと管理tmuxも終了するため、自動化の実行対象はM.A.R.S.稼働中とする。
- 初期MVPでは「予定時刻での再起動」を対象とする。予期しないクラッシュからの自動復旧は、実行基盤をsystemdサービスへ移行する段階で追加する。

### 3. 自動バックアップの設定

- 自動バックアップを有効・無効にできる。
- 自動再起動と連動するか、独立して実行するかを選択できる。
- 連動時は再起動設定の曜日・時刻を共有し、各処理を停止完了確認→バックアップ→起動の順序で実行する。バックアップに失敗しても、可能な限りサーバーを再起動して停止状態を残さない。
- 非連動時は、再起動と同じ形式で月〜日のボタンを複数選択し、選択曜日に共通する時刻（HH:MM、24時間表記）を設定できる。
- バックアップ保存先、保持世代数、保持日数を設定できる。
- GUIから手動バックアップを実行できる。
- 初期の既定保存先は `/home/chappy/empire/data/minecraft-backups` とする。
- 実行前に保存先、空き容量、サーバーディレクトリを検証する。
- 稼働中は `save-off`、`save-all flush` を送り、送信後の保存完了ログを確認してからワールドをアーカイブする。成否にかかわらず `save-on` の復旧を試みる。
- 一時ファイルへ作成し、検証後に完成名へ変更する。失敗したアーカイブを成功扱いしない。
- 各バックアップに作成日時、対象ワールド、Minecraft/Forgeバージョン、サイズ、ハッシュ、実行結果を記録する。
- 世代削除は新しいバックアップの成功後だけ行い、最新の正常バックアップは削除しない。
- バックアップ処理と再起動処理が同時に走らないよう共通ロックを使う。
- 復元GUIは初期MVPの対象外だが、手動復元できる形式と手順を残す。

## 非機能要件

### 安全性

- パスや時刻などの入力値を検証し、シェル文字列連結によるコマンド実行を避ける。
- ワールド削除、上書き復元、EULA同意、ポート公開を自動実行しない。
- サーバー停止時はタイムアウトを設け、強制終了は明示的な確認なしに行わない。
- バックアップ対象と削除対象は正規化した絶対パスで検証する。

### 信頼性

- アプリや自動処理の異常終了後も、次回起動時に処理結果と設定を確認できる。
- 自動処理は一意な実行IDを持ち、開始、成功、失敗、所要時間を記録する。
- ログやワールドが存在しない初期状態でもGUIがクラッシュしない。
- サーバーが既に起動中または停止中の場合、操作を冪等に扱う。

### 性能・操作性

- 通常の状態更新でGUIスレッドをブロックしない。
- 起動状態は通常2秒以内、ログ追従は通常1秒以内に画面へ反映する。
- 初期対象は1台のローカルサーバー、最大5プレイヤー程度とする。

### テスト

- サーバー操作、スケジュール生成、バックアップ、保持世代、安全なパス検証を単体テストする。
- 一時ディレクトリと偽アダプターを使い、実ワールドを変更しない統合テストを用意する。
- 実サーバーを使うテストは明示的に分離し、通常のテストでは起動・停止しない。

## MVP受け入れ条件

1. Ubuntu上で一般ユーザー権限のままGUIを起動できる。
2. 現在のForgeサーバーの状態とログをGUIで確認できる。
3. GUIから正常な起動、停止、再起動を実行できる。
4. 自動再起動の有効化と複数曜日・時刻設定ができ、M.A.R.S.稼働中は予定時刻までに8回の事前警告がゲーム内へ届き、最後に安全な再起動が行われる。M.A.R.S.終了中は管理tmuxを再作成せずスキップする。
5. 自動バックアップを再起動連動または独立複数曜日・時刻に設定でき、保存先・保持設定を変更して手動・定期の両方で正常なアーカイブを作成できる。
6. 失敗時にワールドや直近の正常バックアップを失わず、原因を履歴で確認できる。
7. コア機能の自動テストが通り、起動・運用・手動復元手順が文書化されている。
8. 稼働中にウィンドウを閉じる操作で確認ダイアログが表示され、NOで停止・終了せず、YESで安全停止後に終了する。

## 初期対象外

- インターネット越しの遠隔管理、Web UI、スマートフォン対応。
- 複数サーバーの同時管理。
- MODの自動更新。
- `server.properties` やMOD設定の高度な編集。
- バックアップのGUI復元。
- RCONの自動有効化と認証情報管理。
- Windows/macOS対応。

これらはアーキテクチャ上の拡張余地を残すが、初期MVPを遅らせる依存要件にはしない。

## 開発フェーズ

| フェーズ | 内容 | 完了条件 |
| --- | --- | --- |
| 0 | 要件・記録基盤 | 本文書と進捗DBが相互参照されている |
| 1 | アプリ骨格・読み取り専用監視 | GTK画面で状態とログを確認できる |
| 2 | 手動操作 | 起動、停止、再起動を安全に実行できる |
| 3 | 自動再起動 | 設定、systemdタイマー、履歴、失敗処理が動く |
| 4 | バックアップ | 手動・定期バックアップと保持処理が動く |
| 5 | 堅牢化・配布 | テスト、文書、パッケージ、実環境確認が完了する |

## 今後ユーザーと確定する事項

- 自動再起動の既定時刻・曜日と、警告メッセージを将来ユーザー編集可能にするか。
- バックアップの既定頻度、保存先、保持世代数、圧縮形式。
- GUIの配色、アイコン、中央領域とコンソール領域の比率。
- ログインしていない間も自動運用するためのsystemd linger設定を許可するか。
- クラッシュ時の自動復旧をMVPへ前倒しするか。
